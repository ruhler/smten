
module Smten.Sudoku.Solver where

import Smten.SMT.Symbolic
import Smten.Sudoku.Cell
import Smten.Sudoku.Board

solve :: (Eq c, Cell c) => Symbolic (Board c) -> SMT (Maybe (Board c))
solve board = query $ do
    b <- board
    assert (isvalid b)
    return b
    
