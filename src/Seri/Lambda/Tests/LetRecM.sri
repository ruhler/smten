

module Seri.Lambda.Tests.LetRecM where {

import Seri.Lib.Prelude;

-- Recursive let blocks are not allowed.
-- We shouldn't do any silent shadowing or such that would differ from haskell
-- semantics.
foo :: Integer -> Integer;
foo a =
  let { b = a + 1;
        a = 3;
      } in a;

}

