
module Seri.Lambda.Tests.InstCtx where

import Prelude

data Foo = Foo | Bar

-- The Maybe type instance of Eq has a context.
-- We expect this not to type check because there is no instance of Eq for
-- Foo, even though there is an instance for Maybe.
foo :: Maybe Foo -> Bool
foo m = m == Just Foo

