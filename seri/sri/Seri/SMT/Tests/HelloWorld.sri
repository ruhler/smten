
module Seri.SMT.Tests.HelloWorld where

import Prelude
import Seri.SMT.SMT

qmain :: Query (Answer Integer)
qmain = do
    x <- free
    assert (x < 6)
    assert (x > 4)
    query x

main :: IO ()
main = do
    r2 <- runYices2 (Just "HelloWorld.dbg") qmain
    putStrLn (show r2)

